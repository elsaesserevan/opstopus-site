source 'https://rubygems.org'

gem 'jekyll'
gem 'jekyll-theme-minimal'
gem 'webrick'
gem 'eventmachine', '~> 1.2.7', platforms: [:mswin, :mingw, :x64_mingw]
gem 'wdm', '>= 0.1.0', platforms: [:mswin, :mingw, :x64_mingw]

# GitHub Pages is commented out here for local Windows preview because it pulls Jekyll 3.9/Liquid 4 and is not compatible with this environment.
# gem 'github-pages', group: :jekyll_plugins

# csv, bigdecimal, and base64 are no longer bundled in Ruby's stdlib by default; add them as gems
gem 'csv'
gem 'bigdecimal'
gem 'base64'
